# netexec-automator-on-crack
Automates NetExec across all protocols and auth methods, summarises results, and suggests follow-up commands based on what succeeded.

# set up

Download the .py 

chmod +x netexec-automator-2.0.py

chmod +x /path/to/netexec-automator-2.0.py

sudo cp /path/to/netexec-automator.py /usr/local/bin/netexec-automator
